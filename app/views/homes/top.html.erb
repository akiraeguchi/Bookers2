<%= render 'layouts/notification', text_color: "text-success" %>

<div class="container">
  <div class="row d-flex mr-15 ml-15">
    <div class="mx-auto col-11 col-sm-10 col-lg-8 col-xl-6">
      <h1>welcome to <i class="fas fa-book"></i><strong>Bookers</strong> !!</h1>
      <p>In <i class="fas fa-book"></i><strong>Bookers</strong>,</p>
      <p>you can share and exchange your opinions, inpressions, and emotions</p>
      <p>about various books!</p>
        <div class="btn-wrapper col-11 mx-auto">
          <div class="row-lg-10">
            <% if user_signed_in? %>
              <%= link_to "Log in", user_path(@user), class: "btn btn-info btn-sm btn-block mb-3" %>
            <% else %>
            <%= link_to "Log in", new_user_session_path, class: "btn btn-info btn-sm btn-block mb-3" %>
            <% end %>
          </div>
          <div class="row-lg-10">
            <% if user_signed_in? %>
            <%= link_to "Sign up", user_path(@user), class: "btn btn-success btn-sm btn-block" %>
            <% else %>
            <%= link_to "Sign up", new_user_registration_path, class: "btn btn-success btn-sm btn-block" %>
            <% end %>
          </div>
        </div>
      </div>
  </div>
</div>